
---

## 1. JVM, Memory & Execution

- 🟢 What is the difference between JDK, JRE, and JVM?
- 🟢 Why is Java platform independent?
- 🟢 Difference between Heap and Stack memory?
- 🟢 Where are local, instance, and static variables stored?
- 🟡 Explain JVM architecture.
- 🟡 What are the runtime data areas in JVM?
- 🟡 What is the difference between stack and heap during object creation?
- 🟡 What is Metaspace? How is it different from PermGen?
- 🟡 What is ClassLoader? What are the types of ClassLoaders?
- 🟡 What is JIT compiler? How does it improve performance?
- 🟡 How does Garbage Collection work?
- 🔴 Types of Garbage Collectors (G1, CMS, ZGC — high level)
- 🔴 What causes memory leaks in Java?
- 💡 Your app is crashing with `OutOfMemoryError` — what steps will you take?
- 💡 How will you debug high memory usage in production?

## 2. Java Basics

- 💡 Can we run a Java program without `main`?
- 💡 Can we have multiple `main` methods in a project? In the same class?

## 3. Data Types & Operators

- 🟢 Difference between primitive and non-primitive types?
- 🟢 What is type casting? Widening vs narrowing?
- 🟢 What is autoboxing and unboxing?
- 🟡 What is type promotion in expressions?
- 🟡 What is `instanceof` operator?
- 💡 What happens when you compare wrapper objects using `==`?
- 💡 `Integer a = 127; Integer b = 127;` — is `a == b` true? What about 128?

## 4. Control Flow

- 🟡 What is enhanced for-each loop? When can't you use it? (no index, can't remove from collection)
- 💡 What happens if you forget `break` in a `switch`? (fallthrough)

## 5. OOP & Classes

- 🟢 What is OOP? Explain the 4 pillars.
- 🟢 What is the difference between class and object?
- 🟢 What is a constructor? Types of constructors?
- 🟢 Can constructors be overloaded?
- 🟢 What is the `this` keyword?
- 🟡 Can a constructor be private? Why?
- 🟡 What is the difference between a method and a constructor?
- 🟡 Difference between `new` vs reflection vs cloning for object creation?
- 💡 Why would you use a private constructor? (Singleton, Factory, Utility class)

## 6. Encapsulation

- 🟢 What is encapsulation?
- 🟢 Why do we use getters and setters instead of public fields?
- 🟡 How do you make a class immutable?
- 🟡 Why is immutability useful in multithreading?
- 💡 Why is `String` immutable? (both design and real-world reasons)
- 💡 Can you make an immutable class with mutable fields like `Date` or `List`?

## 7. Inheritance

- 🟢 What is inheritance?
- 🟢 Difference between IS-A and HAS-A?
- 🟡 Why Java doesn't support multiple inheritance with classes?
- 🟡 What is the diamond problem?
- 💡 Can a child class constructor run without calling parent constructor?

## 8. Polymorphism

- 🟢 What is polymorphism?
- 🟢 Difference between method overloading and overriding?
- 🟡 What is runtime polymorphism? How does JVM resolve method calls?
- 🟡 Can static methods be overridden?
- 🟡 What is method hiding?
- 🟡 Can you overload the `main` method?
- 💡 What is the output? (Parent ref = new Child(); ref.method() — overloaded vs overridden)
- 💡 Can you override a private method? What actually happens?

## 9. Abstraction & Interfaces

- 🟢 What is abstraction?
- 🟢 Difference between abstract class and interface?
- 🟡 Can an interface have methods with implementation? (Java 8+)
- 🟡 Can an interface have variables? What kind?
- 🟡 What are default, static, and private methods in interfaces?
- 🟡 When to use abstract class vs interface?
- 💡 Can an interface have a constructor? Why not?
- 💡 What happens if two interfaces have the same default method and a class implements both?

## 10. Important Keywords

- 🟢 What is `final` keyword? How is it used with variables, methods, classes?
- 🟢 Difference between `this` and `super`?
- 🟢 What is `static` keyword?
- 🟡 Difference between `final` and immutability?
- 🟡 What is `static import`?
- 💡 Difference between `final` vs `finally` vs `finalize`?
- 💡 What happens if you declare a class as `final`?
- 💡 Can you make a `static` method `final`? What about `abstract`?

## 11. Object Class & Core Methods

- 🟢 What is `toString()`?
- 🟢 Difference between `==` and `.equals()`?
- 🟡 What is `hashCode()`?
- 🟡 Explain equals and hashCode contract.
- 🟡 How do collections rely on `equals()`? (Set/Map behavior)
- 🔴 What happens if you override `equals()` but not `hashCode()`?
- 💡 You put custom objects in a `HashSet` — duplicates appear. Why?

## 12. Strings

- 🟢 Why is String immutable?
- 🟢 Difference between String, StringBuilder, and StringBuffer?
- 🟡 What is String Constant Pool?
- 🟡 How does String Pool work internally? (literals vs `new String()`, when objects are reused)
- 🟡 What is `intern()` method?
- 💡 `String s1 = "abc"; String s2 = new String("abc");` — how many objects created? Are they equal?
- 💡 Is `StringBuilder` thread-safe? When would you use `StringBuffer` in practice?

## 13. Arrays

- 🟢 `length` vs `length()` vs `size()`? (array vs String vs Collection)

## 14. Collections Framework

**Concepts:**

- 🟢 What is Java Collection Framework?
- 🟡 What is `Iterable` and `Iterator`? Difference between `Iterator` and `ListIterator`?
- 🟡 `Collection` vs `Collections`?
- 🟡 What is `Comparable` vs `Comparator`?
- 🟡 `Comparator.comparing()` with `thenComparing()` — multi-field sort chaining

**Differences:**

- 🟢 Difference between array and `ArrayList`? (fixed size, no generics at runtime, no utility methods)
- 🟢 Difference between List vs Set vs Map?
- 🟢 Difference between ArrayList vs LinkedList?
- 🟡 Difference between HashMap vs TreeMap vs LinkedHashMap?
- 🟡 Difference between HashSet vs TreeSet vs LinkedHashSet?

**HashMap Internals:**

- 🟡 Explain `put()` flow step-by-step (hash generation → index calculation → collision handling → tree conversion → resizing)
- 🟡 What is load factor? What is default capacity?
- 🟡 How are collisions handled?
- 🔴 What happens during rehashing?
- 🔴 Java 8: LinkedList → Red-Black Tree conversion — when and why?
- 🔴 Difference between fail-fast and fail-safe iterators?

**Concurrency:**

- 🔴 Difference between `ConcurrentHashMap` vs `HashMap`?
- 💡 Why does `HashMap` allow one null key but `ConcurrentHashMap` doesn't?
- 💡 Why is `HashMap` not thread-safe? What breaks?
- 💡 When would you use `ConcurrentHashMap` over `HashMap`?

## 15. Generics

- 🟢 What are generics? Why use them?
- 🟡 What is a generic class vs generic method?
- 🟡 What is type erasure?
- 🟡 What are wildcards? (`?`, `? extends T`, `? super T`)

## 16. Functional Programming (Java 8)

- 🟢 What is a functional interface?
- 🟢 What is lambda expression?
- 🟡 What is method reference? Types?
- 🟡 Difference between Consumer vs Predicate vs Function vs Supplier?
- 🟡 What is `@FunctionalInterface` annotation?
- 💡 Can a functional interface have multiple methods? (default/static don't count)

## 17. Streams API

- 🟢 What is a Stream? How is it different from Collection?
- 🟡 What are intermediate vs terminal operations?
- 🟡 What is lazy evaluation in Streams?
- 🟡 Commonly used: `map`, `filter`, `reduce`, `collect`, `sorted`, `distinct`
- 🔴 What is `flatMap`? When do you use it?
- 🔴 Difference between `map()` vs `flatMap()`?
- 🔴 Common `Collectors`: `toList`, `toSet`, `toMap`, `groupingBy`, `partitioningBy`, `joining`
- 🔴 `Collectors.toMap` with duplicate keys — what happens, how to fix? (merge function)
- 🔴 What are side effects in streams? Why avoid them?
- 🔴 When would you use parallel streams? What are the risks? (fork-join pool, shared resources)
- 💡 When do you use Streams vs for-loops? (performance vs readability)
- 💡 Why should streams not modify shared state?

## 18. Exception Handling

- 🟢 Difference between checked and unchecked exceptions?
- 🟢 Difference between Error and Exception?
- 🟢 Difference between `throw` and `throws`?
- 🟡 What is try-with-resources?
- 🟡 What is exception propagation?
- 🟡 When do you create custom exceptions?
- 🔴 Should service layer throw checked or unchecked exceptions?
- 🔴 Exception handling best practices
- 🔴 What is the checked exception design problem in Java?
- 💡 Can `finally` block not execute? When? (System.exit())
- 💡 What happens if both `catch` and `finally` throw exceptions?
- 💡 Can you `catch` `Error`? Should you?

## 19. Multithreading & Concurrency

- 🟢 Difference between process and thread?
- 🟢 What is thread safety? (stateless vs stateful classes)
- 🟢 Difference between `Thread` class and `Runnable` interface?
- 🟢 What is thread lifecycle?
- 🟡 What is synchronization? Method-level vs block-level?
- 🟡 What is `volatile`? When to use?
- 🟡 What is race condition? Give an example.
- 🟡 What is deadlock? How to prevent?
- 🟡 Difference between concurrency and parallelism?
- 🟡 Difference between synchronous and asynchronous execution?
- 🟡 Difference between `wait()` and `sleep()`?
- 🟡 What are Atomic classes? (AtomicInteger, AtomicLong, CAS) When to use vs volatile vs synchronized?
- 🟡 Can you call `start()` twice on the same thread? What happens?
- 🟡 Difference between `start()` and `run()`?
- 🟡 What are types of thread pools? (Fixed, Cached, Single, Scheduled)
- 🔴 Difference between `synchronized` vs `Lock` (ReentrantLock)?
- 🔴 What is `ExecutorService`? Why use thread pools?
- 🔴 Difference between `Callable` and `Runnable`?
- 🔴 What is `Future`? How to get results from threads?
- 🔴 What is `CompletableFuture`? How is it different from `Future`?
- 🔴 `CompletableFuture` chaining: `thenApply` vs `thenCompose` vs `thenCombine` vs `allOf`?
- 🔴 What is `ThreadLocal`? Where is it used? (Spring Security holds `SecurityContext` in one; request-scoped data)
- 🔴 What is the Java Memory Model (JMM)? What is the happens-before relationship?
- 🔴 What is `ForkJoinPool`? How does work-stealing work? (powers parallel streams)
- 🔴 Object-level locking vs class-level locking?
- 💡 How would you make a class thread-safe?
- 💡 When would you use async over sync in a backend application?

## 20. File Handling

- 🟢 Difference between byte stream and character stream?
- 🟡 How to read a file line by line? (`BufferedReader`, `Files.lines()`)
- 🟡 `BufferedReader` vs `BufferedWriter` — purpose and why buffering matters (perf)
- 🟡 `FileReader` vs `InputStreamReader` — when to use which? (charset handling)
- 🔴 What is NIO? `Path` vs `File` — why the `java.nio.file` API is preferred now
- 🔴 `Files.readAllLines()` vs `Files.lines()` — memory implications for large files
- 💡 Why use try-with-resources for file handling?

## 21. Serialization

> Java's built-in `Serializable` is rarely asked now — most real serialization is JSON via Jackson. Know the basics only.

- 🟢 What is serialization and deserialization? (concept, not `Serializable`-specific)
- 🟡 What is `transient` keyword?
- 💡 How does Jackson serialize Java objects to JSON? (`ObjectMapper`, `@JsonProperty`, `@JsonIgnore`)

## 22. Annotations

- 🟢 What are annotations?
- 🟡 Explain `@Override`, `@Deprecated`, `@SuppressWarnings`
- 🟡 What happens if you don't use `@Override`? (compile-time safety benefit)
- 🟡 What is `@FunctionalInterface`?
- 🔴 What are meta-annotations? (`@Target`, `@Retention`)
- 🔴 How to create a custom annotation?

## 23. Reflection

- 🟡 What is reflection? (inspect/modify classes, fields, methods at runtime)
- 🟡 Where is it used in practice? (Spring DI, JPA, Jackson, JUnit)
- 🔴 How does `@Autowired` work under the hood?
- 🔴 What are the performance and security downsides of reflection?
- 💡 When have you (or would you) use reflection directly?

## 24. Modern Java

- 🟡 What is `Optional`? Why use it instead of null checks?
- 🟡 Common `Optional` methods: `of()`, `ofNullable()`, `isPresent()`, `orElse()`, `map()`
- 🟡 Difference between `orElse()` and `orElseGet()`? Why does it matter?
- 🟡 Date/Time API (Java 8+): `LocalDate`, `LocalDateTime`, `ZonedDateTime`, `Instant` — when to use which?
- 🟡 `Period` vs `Duration` — what's the difference?
- 🟡 Why was the legacy `Date` / `Calendar` API replaced? (mutability, thread safety, broken API)
- 🟡 What is `var` keyword? (Java 10)
- 🟡 What are text blocks? (Java 15)
- 🟡 What are switch expressions? (Java 14) — arrow syntax, exhaustiveness
- 🟡 What is pattern matching for `instanceof`? (Java 16)
- 🟡 What is pattern matching for `switch`? (Java 21)
- 🟡 What are records? (Java 16)
- 🟡 What are sealed classes? (Java 17)
- 🟡 What are virtual threads? (Java 21) — difference from platform threads, when useful
- 💡 Can `Optional` be used as a method parameter? Should it?

## 25. Design & Best Practices (Java-Specific)

> SOLID, generic design patterns (Singleton, Factory, Strategy…), coupling/cohesion, and LLD practice prompts live in **[3. System Design.md](3.%20System%20Design.md) — Section 13 LLD / OOD**. This section covers Java-specific design mechanics.

- 🟡 What is object pooling? Where is it used in Java? (connection pools, thread pools)
- 🟡 How do you make a class immutable in Java? (`final` class, `final` fields, no setters, defensive copies for mutable fields)
- 🟡 Why is immutability important? (thread safety, safe as `HashMap` keys, caching)
- 🟡 Builder pattern in Java — when to use it? (many optional fields, fluent APIs; `StringBuilder`, `Lombok @Builder`)
- 💡 How would you design a thread-safe singleton in Java? (double-checked locking with `volatile`, `Holder` idiom, enum)
- 💡 Your `HashMap` is slow in production — what could be wrong? (bad `hashCode`, missing `equals`, mutable keys, load factor)