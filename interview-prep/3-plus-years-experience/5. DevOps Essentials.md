
---

## 1. Git

**Basics:**

- 🟢 What is the staging area? Difference between working directory vs staging vs commit?
- 🟢 Difference between `git fetch` vs `git pull`?
- 🟢 Difference between `git merge` vs `git rebase`?
- 🟢 What is a merge conflict? How do you resolve it?
- 🟢 What is `.gitignore`?

**Branching & Workflow:**

- 🟡 What are branching strategies? (GitFlow, trunk-based, feature branching)
- 🟡 Difference between `main`/`master`, `develop`, `feature`, `hotfix` branches?
- 🟡 What is a Pull Request / Merge Request? Why use them?
- 🟡 What is code review? Why is it important?
- 🟡 What is `git stash`? When do you use it?
- 🟡 What is `git cherry-pick`? When to use it?

**Advanced:**

- 🔴 What is `git rebase -i` (interactive rebase)? When to use it?
- 🔴 What is `git bisect`? How to find a bug with it?
- 🔴 What is `git reflog`? How can it save you?
- 🔴 Difference between `git reset` vs `git revert`?
- 🔴 What is squashing commits? Why do teams do it?
- 💡 When should you NOT use rebase? (shared branches trap)

**Scenarios:**

- 💡 You accidentally committed to `main` instead of a feature branch — how do you fix it?
- 💡 You need to undo the last commit but keep the changes — what do you do?
- 💡 Two developers edited the same file — how do you handle the merge conflict?
- 💡 Your branch is 50 commits behind `main` — merge or rebase? Why?

## 2. Docker

**Basics:**

- 🟢 What is Docker? Why use it?
- 🟢 Difference between a container and a virtual machine?
- 🟢 What is a Docker image? What is a container?
- 🟢 What is a Dockerfile? What are common instructions? (`FROM`, `RUN`, `COPY`, `CMD`, `EXPOSE`)
- 🟢 What is Docker Hub?

**Working with Docker:**

- 🟡 What is `docker-compose`? Why use it?
- 🟡 What are Docker volumes? Why are they needed?
- 🟡 What is a Docker network? How do containers communicate?
- 🟡 What is port mapping? (`-p 8080:80`) How does host ↔ container communication work?
- 🟡 `ENV` in Dockerfile vs `.env` file vs `docker-compose.yml` — differences?
- 🟡 What is `docker logs`? Where do container logs go?
- 🟡 Difference between `CMD` vs `ENTRYPOINT`?
- 🟡 Difference between `COPY` vs `ADD`?
- 🟡 What is a `.dockerignore` file?
- 🟡 What is a multi-stage build? Why use it?

**Advanced:**

- 🔴 How do you optimize a Docker image size?
- 🔴 What is Docker layer caching? How does it affect build speed?
- 🔴 What is the `HEALTHCHECK` instruction? Why does production need it?
- 🔴 Why run containers as a non-root `USER`? (security baseline)
- 🔴 What is container orchestration? (Kubernetes — conceptual only)
- 🔴 Difference between Docker Compose vs Kubernetes? (high level)

**Scenarios:**

- 💡 Your app works locally but fails in Docker — what do you check?
- 💡 How do you set up a local dev environment with Docker for a Java/Spring Boot or Laravel app?
- 💡 How do you manage environment variables in Docker? (`.env`, `docker-compose.yml`)
- 💡 Your Docker image is 2GB — how do you reduce it?
- 💡 Difference between `docker run` vs `docker-compose up`?

## 3. CI/CD

**Basics:**

- 🟢 CI vs CD — what's the difference?
- 🟢 Continuous Delivery vs Continuous Deployment — difference?
- 🟢 Why is CI/CD important?
- 🟢 What is a CI/CD pipeline?

**Pipeline Concepts:**

- 🟡 What are typical stages in a CI/CD pipeline? (build, test, lint, deploy)
- 🟡 What are CI/CD tools? (GitHub Actions, Bitbucket Pipelines, Jenkins, GitLab CI)
- 🟡 What is a build artifact?
- 🟡 What is artifact storage? (S3, pipeline storage — where builds are kept)
- 🟡 What are environment variables in CI/CD? How to manage secrets?
- 🟡 What is a pipeline trigger? (push, PR, schedule)
- 🟡 Why cache dependencies in CI? (`composer install`, `npm install`) How it speeds up pipelines?

**Advanced:**

- 🔴 What is blue-green deployment?
- 🔴 What is canary deployment?
- 🔴 What is rolling deployment?
- 🔴 What is zero-downtime deployment? How to achieve it?
- 🔴 What is trunk-based development? How do feature flags enable it?
- 🔴 What is infrastructure as code? (conceptual — Terraform, CloudFormation)

**Scenarios:**

- 💡 How would you set up a basic CI pipeline for a Spring Boot / Laravel app?
- 💡 A deployment breaks production — how do you rollback?
- 💡 How do you run tests automatically before merging a PR?
- 💡 How do you handle database migrations in CI/CD?

## 4. Cloud Essentials (AWS-Focused)

**Core Concepts:**

- 🟢 What is cloud computing? Why use it?
- 🟢 Difference between IaaS, PaaS, and SaaS?
- 🟢 What are regions and availability zones?

**Compute & Networking:**

- 🟡 What is EC2? How does it work? (high level)
- 🟡 What is a load balancer? Why use it?
- 🟡 What is auto-scaling? (conceptual)
- 🟡 What is a VPC? (conceptual — your app lives inside it)

**Storage & Database:**

- 🟡 What is S3? Common use cases?
- 🟡 What is RDS? Why use managed databases?
- 🟡 Difference between SQL (RDS) vs NoSQL (DynamoDB)? When to use each?

**Containers on AWS:**

- 🟡 What is ECS? What is Fargate? (managed container service, serverless containers)
- 🟡 What is EKS? When would you choose it over ECS?
- 💡 How would you deploy a Dockerized Spring Boot / Laravel app to AWS? (ECS Fargate behind ALB)

**Serverless & Others:**

- 🟡 What is Lambda? When would you use serverless?
- 🟡 What is CloudWatch? How do you use it for monitoring/logging?
- 🟡 How do you query logs at scale? (CloudWatch Logs Insights)
- 🟡 What is Cognito? When to use it? (you've used this)

**Security & IAM (High Level):**

- 🟡 What is IAM? Difference between **users**, **roles**, and **policies**?
- 🟡 When do you attach a role to an EC2/ECS task vs give a user access keys?
- 🟡 What is the principle of least privilege?
- 🟡 How do you manage secrets in AWS? (Secrets Manager, Parameter Store — pick which and why)

**Advanced:**

- 🔴 What is a CDN? What is CloudFront?
- 🔴 What is SQS? SNS? When to use message queues?
- 🔴 What is an API Gateway in AWS context?
- 🔴 Difference between horizontal vs vertical scaling?
- 🔴 Why must apps be stateless in the cloud? Where do you store state? (DB, Redis, S3)
- 🟡 What is a reverse proxy? What is Nginx doing in deployment?
- 🟡 What is a health check? Why is it important in deployments?

**Scenarios:**

- 💡 How would you deploy a Spring Boot / Laravel app to AWS? (high-level architecture)
- 💡 Your app is getting slow under high traffic — what AWS services would you use?
- 💡 How do you handle logs and monitoring in a cloud environment?
- 💡 Why does updating `.env` not reflect in production? (config caching, container rebuild needed — connects Laravel + Docker + deployment)
- 💡 Difference between deploying on EC2 vs using Lambda?

## 5. Kubernetes (Conceptual — Awareness Level)

> You don't need to admin clusters, but senior roles increasingly ask "can you read a pod spec?"

- 🟡 What is Kubernetes? How is it different from Docker Compose?
- 🟡 What is a Pod? A Deployment? A Service? An Ingress?
- 🟡 What is a ConfigMap vs a Secret?
- 🟡 What are liveness and readiness probes? Why does Spring Boot Actuator `/health` matter here?
- 🔴 What is a rolling update in Kubernetes?
- 💡 A pod keeps restarting — how do you debug? (`kubectl describe`, `kubectl logs`, probe failures)

## 6. How They All Connect (Big Picture)

- 💡 Walk through a full deployment: developer pushes code → Git → CI/CD pipeline → Docker build → deploy to cloud
- 💡 How does your current/last project handle deployment?
- 💡 What is your local development setup? (Docker Compose, etc.)
- 💡 How do you ensure what works locally also works in production?
- 💡 Why does "works on my machine" happen? How does Docker solve it? (environment parity)
- 💡 Where can things fail in the pipeline? (Git → CI → Docker → Cloud) How do you debug each stage?

## 7. Common Production Issues

- 💡 App works locally but not in production — what could be wrong?
- 💡 Environment variable missing or misconfigured
- 💡 Port issues — app not accessible
- 💡 DB connection failing in production
- 💡 Cache or config not cleared after deployment
- 💡 SSL/HTTPS issues after deployment
- 💡 How do you approach debugging any production issue? (your step-by-step process)

---

> **Note:** As a backend dev, you're expected to _use_ these tools, not _administer_ them. Focus on understanding what each tool does, when to use it, and how they fit together — not deep DevOps internals.