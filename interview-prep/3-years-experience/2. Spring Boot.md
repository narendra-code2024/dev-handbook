
---

## Spring Core (IoC & DI)

- 🟢 What is Inversion of Control (IoC)?
- 🟢 What is Dependency Injection (DI)?
- 🟢 Types of DI in Spring? (Constructor, Setter, Field)
- 🟢 Difference between constructor injection vs setter injection?
- 🟡 What is ApplicationContext?
- 🟡 Difference between BeanFactory and ApplicationContext?
- 🔴 How does Spring create beans internally?
- 🔴 What is IoC container doing behind the scenes?
- 💡 Why is DI better than creating objects using `new`?
- 💡 Why is field injection discouraged? What should you use instead?

## Spring Boot Basics

- 🟢 What is Spring Boot?
- 🟢 How is Spring Boot different from Spring Framework?
- 🟢 What is starter dependency?
- 🟢 What is `@SpringBootApplication`?
- 🟡 What annotations does `@SpringBootApplication` include?
- 🟡 What is auto-configuration?
- 🔴 How does auto-configuration work internally?
- 🔴 How do you debug auto-configuration? (`--debug`, condition evaluation report)
- 💡 How does Spring Boot know which beans to create automatically?

## Bean Lifecycle & Scopes

- 🟢 What is a Spring Bean?
- 🟡 Explain the full bean lifecycle steps.
- 🟡 What are bean scopes? (Singleton, Prototype, Request, Session)
- 🟡 Difference between Singleton and Prototype scope?
- 🔴 What are `@PostConstruct` and `@PreDestroy`?
- 💡 Difference between singleton in Java vs singleton in Spring?
- 💡 Where would you initialize DB connections — constructor or `@PostConstruct`?

## Bean Scanning & Stereotypes

- 🟢 What is component scanning?
- 🟢 Difference between `@Component`, `@Service`, `@Repository`, `@Controller`?
- 🟡 What is `@ComponentScan`?
- 🟡 What is `@Configuration` and `@Bean`?
- 🟡 Why is `@Repository` special? (exception translation — DB exceptions → DataAccessException)
- 💡 What happens if a bean is not picked up by Spring? How do you debug?

## Dependency Injection (Advanced)

- 🟢 What is `@Autowired`?
- 🟡 What is `@Qualifier`?
- 🟡 What is `@Primary`?
- 🟡 Difference between `@Primary` vs `@Qualifier`?
- 🟡 What happens if multiple beans of same type exist?
- 🔴 How do you inject different implementations dynamically?
- 💡 How do you resolve bean ambiguity in a real project?

## Spring AOP (Aspect-Oriented Programming)

- 🟡 What is AOP?
- 🟡 What are Advice, Aspect, JoinPoint, and Pointcut?
- 🟡 Types of advice? (Before, After, Around, AfterReturning, AfterThrowing)
- 🔴 How does Spring AOP work internally? (proxy-based)
- 🔴 Difference between Spring AOP and AspectJ?
- 💡 Where have you used AOP in real projects? (logging, transactions, security)
- 💡 How does `@Transactional` relate to AOP?

## Spring MVC (Web Layer)

- 🟢 What is Spring MVC?
- 🟢 What is `DispatcherServlet`?
- 🟢 `@Controller` vs `@RestController`?
- 🟢 `@RequestMapping` vs `@GetMapping` / `@PostMapping`?
- 🟡 Explain complete HTTP request flow (Client → DispatcherServlet → Controller → Service → Repository → DB)
- 🟡 What is `@RequestBody` vs `@RequestParam` vs `@PathVariable`?
- 🔴 What is HandlerMapping and HandlerAdapter?
- 🔴 What is `ResponseEntity` and when to use it?
- 💡 How does Spring know which controller method to call for a request?
- 💡 Why should controllers not contain business logic?

## REST APIs & Best Practices

- 🟡 Difference between PUT vs PATCH? (with real use cases)
- 🟡 How do you design a RESTful API properly?
- 🟡 What is idempotency? Which HTTP methods are idempotent?
- 🟡 Difference between Entity vs DTO? Why should you not expose entities in APIs?
- 🔴 How do you version APIs? (URI, header, query param approaches)
- 🔴 What is HATEOAS? Have you used `spring-hateoas`? (rare but senior-level ask)
- 💡 Design an API for orders with pagination, filtering, and sorting.
- 💡 How do you handle partial updates in REST?

## Spring Data JPA / Hibernate

- 🟢 What is JPA?
- 🟢 What is Hibernate? Difference between JPA and Hibernate?
- 🟢 What is an Entity? (`@Entity`, `@Id`, `@GeneratedValue`)
- 🟡 What are relationship mappings? (`@OneToMany`, `@ManyToOne`, `@ManyToMany`)
- 🟡 Difference between unidirectional and bidirectional mapping? What is `mappedBy`?
- 🟡 Difference between `CrudRepository` vs `JpaRepository`?
- 🟡 What are derived query methods? (e.g., `findByEmail`)
- 🟡 What is `@Query` annotation? (JPQL vs Native query)
- 🟡 How does pagination work in Spring Data JPA? (`Pageable` and `Page`)
- 🟡 How do you call a stored procedure from Spring Boot? (`@Query` native, `@Procedure`, `EntityManager`)
- 🔴 What is lazy vs eager loading? (with real issue explanation)
- 🔴 What is the N+1 problem? How to solve it? (`JOIN FETCH`, `@EntityGraph`, batch size)
- 🔴 `@EntityGraph` vs `JOIN FETCH` — when to use which?
- 🔴 What is `EntityManager`?
- 🔴 What is persistence context? (First-level cache)
- 🔴 What is dirty checking in Hibernate?
- 🔴 What is Open Session in View (OSIV)? Why can it cause performance issues?
- 🔴 How do you investigate a slow JPA query? (enable `show-sql` / `format_sql`, grab the SQL, run `EXPLAIN`, add/fix indexes)
- 🔴 Composite index leftmost-prefix rule — why does your query not use the index?
- 💡 Your API is slow due to DB calls — what will you check?
- 💡 How would you optimize a slow JPA query?

## Transactions

- 🟢 What is a transaction?
- 🟢 What are ACID properties?
- 🟡 What is `@Transactional`?
- 🟡 What is transaction propagation? (REQUIRED, REQUIRES_NEW, etc.)
- 🟡 What is transaction isolation level?
- 🔴 How does `@Transactional` work internally? (Proxy mechanism / AOP)
- 🔴 What happens when a transactional method calls another transactional method internally?
- 💡 What happens if an unchecked vs checked exception occurs inside a transaction?
- 💡 Why does `@Transactional` sometimes not work? (self-invocation, private methods)

## Validation

- 🟢 What is Bean Validation?
- 🟢 What is `@Valid` and `@Validated`?
- 🟢 Common validation annotations: `@NotNull`, `@NotBlank`, `@Size`, `@Email`, `@Min`, `@Max`
- 🟡 How do you create custom validation annotations?
- 🟡 How do you handle validation errors and return proper responses?
- 💡 Difference between `@NotNull`, `@NotEmpty`, and `@NotBlank`?

## Exception Handling

- 🟢 How do you handle exceptions globally in Spring Boot?
- 🟢 What is `@ControllerAdvice`?
- 🟢 What is `@ExceptionHandler`?
- 🟡 How do you return custom API error responses? (error DTO pattern)
- 🟡 Difference between `@ControllerAdvice` and `@RestControllerAdvice`?
- 🔴 How do you design a standardized error response structure for production APIs?
- 💡 Should you use checked or unchecked exceptions in service layer?

## Configuration & Profiles

- 🟢 What is `application.properties` vs `application.yml`?
- 🟢 What are Spring Profiles?
- 🟡 How do you switch between environments (dev/staging/prod)?
- 🟡 What is `@Value` annotation?
- 🔴 What is `@ConfigurationProperties`? Why prefer it over `@Value`?
- 🔴 How do you validate `@ConfigurationProperties`? (`@Validated` + JSR-303 annotations)
- 🔴 How do you externalize configuration? What is Spring Cloud Config (conceptually)?
- 💡 How do you store secrets securely? (not hardcode in properties)

## Spring Security (Basics)

- 🟢 What is Spring Security?
- 🟢 Difference between authentication and authorization?
- 🟡 What is JWT? How does JWT authentication work?
- 🟡 What is the Spring Security filter chain?
- 🟡 What is CORS? How do you configure it?
- 🔴 How do you implement role-based access control?
- 🔴 Method-level security: `@PreAuthorize`, `@PostAuthorize`, `@Secured` — how do they differ?
- 🔴 Difference between AuthenticationManager and UserDetailsService?
- 🔴 Why should passwords be encoded? (BCrypt)
- 🔴 OAuth2 / OIDC basics — what is a resource server vs authorization server? How does Spring Security resource-server config look?
- 💡 How would you secure REST APIs in a real project?
- 💡 Where do you store JWT tokens — localStorage vs httpOnly cookie?

## Caching & Performance

- 🟢 What is caching? Why use it?
- 🟡 What is `@Cacheable`, `@CacheEvict`, `@CachePut`?
- 🟡 What is connection pooling? (HikariCP) — how do you tune pool size?
- 🟡 Local vs distributed cache — Caffeine (in-process) vs Redis vs Ehcache? When to use which?
- 🔴 What is `@Async`? How does Spring manage threads? (`TaskExecutor`)
- 🔴 How do you improve API performance? (caching, DB optimization, connection pooling, async)
- 💡 API is slow — what steps will you take to debug?
- 💡 When should you NOT cache?

## Microservices Concepts

- 🟢 What is microservices architecture?
- 🟢 Difference between monolith and microservices?
- 🟡 What is service discovery? (Eureka — conceptually)
- 🟡 What is API Gateway? Why use it?
- 🟡 How do microservices communicate? (REST, message queues)
- 🔴 What is Spring Cloud Config? (externalized config server — conceptually)
- 🔴 What is circuit breaker pattern? How do you use `@CircuitBreaker` / `@Retry` / `@Bulkhead` (Resilience4j)?
- 🔴 What is eventual consistency?
- 💡 When would you choose monolith over microservices?
- 💡 How do you handle distributed transactions? (Saga, outbox pattern)

## Messaging (Kafka / RabbitMQ)

- 🟡 Why use a message broker in a Spring Boot app? (async, decoupling, spikes)
- 🟡 Kafka vs RabbitMQ — when to use which?
- 🟡 `spring-kafka` basics: `KafkaTemplate`, `@KafkaListener`, consumer groups
- 🟡 `spring-amqp` basics: `RabbitTemplate`, `@RabbitListener`, exchanges/queues/bindings
- 🔴 At-least-once vs exactly-once vs at-most-once delivery — which does Kafka give?
- 🔴 How do you handle a failed message? (retry, DLQ — dead letter topic/queue)
- 🔴 How do you guarantee ordering? (partition key in Kafka)
- 💡 You publish an event but the DB transaction rolls back — what's the bug? (outbox pattern)
- 💡 Duplicate messages arrive — how do you make the consumer idempotent?

## Scheduled Tasks

- 🟡 How do you run scheduled tasks in Spring Boot? (`@Scheduled`, `@EnableScheduling`)
- 🟡 Difference between `fixedRate`, `fixedDelay`, and `cron`?
- 🔴 How do you run `@Scheduled` jobs safely across multiple instances? (ShedLock, leader election)
- 💡 Your scheduled job runs twice in every deploy window — why?

## Testing

- 🟢 Difference between unit testing and integration testing?
- 🟡 What is `@SpringBootTest`?
- 🟡 Test slicing: `@WebMvcTest`, `@DataJpaTest`, `@JsonTest` — when over full `@SpringBootTest`?
- 🟡 What is MockMvc? How to test controllers?
- 🟡 What is `@MockBean` vs `@Mock`?
- 🔴 How do you test service layer with mocked dependencies?
- 🔴 How do you test repository layer? (`@DataJpaTest`)
- 🔴 What is TestContainers? Why use it over H2 for integration tests? (real DB, avoids dialect drift)
- 💡 Should you test private methods? Why or why not?

## Actuator & Monitoring

- 🟢 What is Spring Boot Actuator?
- 🟡 What endpoints does it provide? (`/health`, `/info`, `/metrics`, `/prometheus`)
- 🟡 How do you customize actuator endpoints?
- 🟡 What is Micrometer? How does it expose metrics to Prometheus?
- 🔴 How do you monitor a Spring Boot app in production? (Actuator + Micrometer + Prometheus + Grafana)
- 🔴 How do you add custom metrics? (`MeterRegistry`, `Counter`, `Timer`)

## Debugging & Real-World Problems (Game Changers)

- 🔴 API is slow — what are your debugging steps?
- 🔴 DB query is slow — how do you investigate?
- 🔴 High CPU usage — what do you check?
- 🔴 Memory leak in Spring Boot app — how do you detect and fix?
- 🔴 Bean creation fails at startup — how do you debug?
- 💡 Your `@Transactional` is not rolling back — what could be wrong?
- 💡 Lazy loading throws `LazyInitializationException` — why and how to fix?
- 💡 API returns 403 but user is authenticated — what do you check?

## Reactive (WebFlux) — Awareness Level

> An alternate paradigm, not core Spring Boot. Know it exists and when it applies — interviewers often ask "have you used reactive?"

- 🟡 What is Spring WebFlux? How is it different from Spring MVC? (non-blocking, reactive streams)
- 🟡 `Mono` vs `Flux` — basic idea
- 💡 When would you actually choose WebFlux over MVC? (I/O-bound, high concurrency, streaming)