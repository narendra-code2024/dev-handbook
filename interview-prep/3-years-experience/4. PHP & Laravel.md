
---

## PHP

### PHP Core Concepts

- 🟢 How does PHP work behind the scenes? (request lifecycle)
- 🟢 Difference between `include` vs `require` vs `include_once` vs `require_once`?
- 🟢 Difference between `isset()` vs `empty()` vs `is_null()`?
- 🟢 What are associative arrays in PHP?
- 🟢 Difference between `==` and `===` in PHP?
- 🟡 Difference between `array_merge()` vs `array_combine()` vs `array_replace()`?
- 🟡 What are `array_filter()`, `array_map()`, and `array_reduce()`?
- 🟡 What are Namespaces and why are they important?
- 🟡 What is the `final` keyword in PHP?
- 🟡 What are PHP PSR Standards? Why do they matter? (PSR-4, PSR-12)
- 🟡 Difference between `echo` and `print`? What about `var_dump()` vs `print_r()`?
- 🟡 What is `serialize()` vs `json_encode()`? When to use each?
- 🔴 What is PHP-FPM? How does it work with Nginx?
- 🔴 Difference between PHP CLI vs FPM?
- 🔴 What is the stateless nature of PHP?
- 💡 What is the difference between `null`, `false`, `0`, `""`, and `[]` in PHP? (type juggling)
- 💡 What does `var_dump("10abc" == 10)` return? Why?
- 💡 What is the `foreach` by reference gotcha in PHP?

### PHP Memory & Internals

- 🟡 How does PHP memory management work?
- 🔴 What are references (`&`) in PHP?
- 🔴 What is copy-on-write in PHP?
- 💡 How would you debug a PHP memory issue?

### Data Types & Variables

- 🟢 What are PHP data types?
- 🟢 Difference between `string`, `int`, `float`, `bool`, `array`, `object`, `null`?
- 🟡 What is type hinting in PHP? (scalar, return types, nullable)
- 🟡 What is strict typing? (`declare(strict_types=1)`)
- 🟡 What are union types and intersection types? (PHP 8+)
- 💡 What happens if you pass a string to a function expecting int without strict mode?

### OOP in PHP

- 🟢 What is the difference between abstract class and interface?
- 🟢 What is polymorphism? What is encapsulation?
- 🟢 What are access modifiers? (`public`, `protected`, `private`)
- 🟡 What is method overloading vs overriding in PHP?
- 🟡 Does PHP support multiple inheritance? How is it handled?
- 🟡 What are Traits? Why and when to use them?
- 🟡 What is Dependency Injection? How to implement it?
- 🟡 What are magic methods? (`__construct`, `__destruct`, `__get`, `__set`, `__call`, `__toString`)
- 🟡 What is late static binding? (`static::` vs `self::`)
- 🔴 Explain SOLID principles with PHP examples.
- 🔴 What are anonymous classes in PHP?
- 💡 When would you choose a Trait over an Interface?
- 💡 Can you use a Trait and an Interface together? When would you?

### PHP Functions & Modern Features

- 🟢 What are closures/anonymous functions in PHP?
- 🟢 What is the `use` keyword in closures?
- 🟡 What are generators in PHP? (`yield`)
- 🟡 What are named arguments? (PHP 8.0)
- 🟡 What is constructor property promotion? (PHP 8.0)
- 🟡 What is the match expression vs switch? (PHP 8.0)
- 🟡 What are attributes? (PHP 8.0) — used for routing, validation, ORM
- 🟡 What are enums? Backed vs pure enums, enums-with-methods (PHP 8.1)
- 🟡 What are `readonly` properties / `readonly` classes? (PHP 8.1 / 8.2)
- 🟡 What is first-class callable syntax? (`strlen(...)`) (PHP 8.1)
- 🟡 What is `new` in initializers? (PHP 8.1)
- 🟡 What are union types, intersection types, DNF types? (PHP 8.0 / 8.1 / 8.2)
- 💡 What is new in PHP 8.x that you've used in a real project?

### Composer & Autoloading

- 🟢 What is Composer?
- 🟡 What is autoloading? How does PSR-4 autoloading work internally?
- 🟡 How does Composer resolve dependencies?
- 🟡 What does `composer dump-autoload` do?
- 💡 Difference between `composer install` vs `composer update`?

### Session, Cookies & Form Handling

- 🟢 Difference between `$_SESSION` vs `$_COOKIE`?
- 🟢 How does PHP handle form data? (`$_GET`, `$_POST`, `$_REQUEST`)
- 🟡 How does session management work internally in PHP?
- 🟡 How to handle file uploads in PHP?
- 💡 How do you prevent session hijacking?

### Error Handling

- 🟢 What are the types of errors in PHP? (Notice, Warning, Fatal)
- 🟡 How does `try-catch-finally` work in PHP?
- 🟡 Difference between errors and exceptions in PHP?
- 🟡 What is a custom exception class?
- 🔴 How does PHP 8 handle errors differently? (`Throwable`, `Error` vs `Exception`)
- 💡 How do you debug a PHP application after an error? (both FE and BE)

### Design Patterns in PHP

- 🟡 What is Singleton pattern? When to use?
- 🟡 What is Factory pattern? When to use?
- 🟡 What is Repository pattern?
- 🟡 What is Strategy pattern?
- 🔴 What is Front Controller pattern?
- 🔴 What is Observer pattern?
- 💡 Which design patterns have you used in real projects?

### Performance & Security

- 🟡 How to optimize the performance of a PHP application?
- 🟡 What is opcode caching? (OPcache)
- 🔴 How to prevent SQL injection? (prepared statements, parameterized queries)
- 🔴 How to prevent XSS (Cross-Site Scripting)?
- 🔴 How to prevent CSRF (Cross-Site Request Forgery)?
- 🔴 What is input sanitization vs validation?
- 💡 How would you secure a PHP application? (give a full checklist)

---

## Laravel

### Laravel Fundamentals

- 🟢 What is Laravel?
- 🟢 What is Artisan? Common artisan commands?
- 🟢 What is the purpose of the `config` folder?
- 🟡 What is `tinker` and what is it used for?
- 🟡 Difference between Laravel and other PHP frameworks? (Symfony, CodeIgniter)
- 🟡 Laravel is built on top of Symfony components — which ones? (HttpFoundation, Console, Routing)

### Laravel Request Lifecycle (Deep)

- 🟡 What is the Laravel request lifecycle? (full flow)
- 🔴 What is the HTTP Kernel? What role does it play?
- 🔴 Service Providers boot sequence — how does it work?
- 🔴 Middleware pipeline execution flow
- 💡 A request hits your app — walk through every step until the response is returned.

### Routing & Middleware

- 🟢 How does routing work in Laravel?
- 🟢 What are route groups? Route parameters?
- 🟡 What is Middleware? How does it work?
- 🟡 Types of middleware? (Global, Route, Group)
- 🟡 How to create custom middleware?
- 🔴 What is the middleware pipeline internally?
- 💡 When would you use middleware vs a controller check?

### Service Container & Providers

- 🟡 What is the Service Container?
- 🟡 What is Dependency Injection in Laravel context?
- 🟡 What is a Service Provider? What is its purpose?
- 🟡 Difference between `register()` and `boot()` methods in Service Provider?
- 🔴 How does Laravel resolve dependencies using reflection?
- 🔴 What is binding? (simple bind, singleton, interface to implementation)
- 🔴 What is contextual binding? (`when()->needs()->give()`)
- 💡 When would you create a custom Service Provider?

### Facades

- 🟡 What are Facades in Laravel?
- 🟡 How do Facades work internally? (static proxy to container)
- 🔴 Difference between Facade vs Helper vs Dependency Injection?
- 💡 When would you use a Facade vs injecting the dependency?

### Eloquent ORM & Database

- 🟢 What is Eloquent ORM?
- 🟢 Difference between `find()` vs `where()`?
- 🟢 What are Migrations? What is Database Seeding?
- 🟡 How to define relationships? (`hasOne`, `hasMany`, `belongsTo`, `belongsToMany`, `morphMany`)
- 🟡 What is Soft Delete?
- 🟡 What is Query Builder? When to use it over Eloquent?
- 🟡 What is eager loading vs lazy loading? (`with()` vs `load()`)
- 🟡 What is lazy eager loading?
- 🟡 What are Accessors and Mutators?
- 🟡 What are Scopes? (local and global)
- 🟡 What is `$casts` property? What are custom casts?
- 🟡 Difference between `firstOrCreate`, `firstOrNew`, `updateOrCreate`?
- 🟡 `pluck()` vs `get()` vs `select()` — differences and when to use each?
- 🟡 `withCount()`, `whereHas()`, `has()`, `doesntHave()` — relationship query helpers
- 🟡 How do you query JSON columns in Eloquent? (`->`, `whereJsonContains`)
- 🔴 What is the N+1 problem? How to solve it? (`with()`, `withCount()`)
- 🔴 Difference between `chunk()` vs `cursor()` vs `lazy()`? When to use each?
- 🔴 What are Model Events? (`creating`, `created`, `updating`, etc.)
- 🔴 What are Observers?
- 💡 Your API is slow because of too many DB queries — how do you fix it?
- 💡 When would you use raw queries over Eloquent?

### Database: Transactions, Indexing & Locking

- 🟡 What is a database transaction?
- 🟡 How to use `DB::transaction()` in Laravel?
- 🟡 What is rollback? When does it happen?
- 🟡 What are indexes? When to use them?
- 🔴 What is pessimistic vs optimistic locking?
- 💡 How would you optimize a slow database query?

### Concurrency & Race Conditions

- 🔴 What is a race condition? Example in Laravel? (double payment, double order)
- 🔴 How to fix race conditions? (DB transactions, locks, queues)
- 🔴 What is atomic locking in Laravel?
- 💡 Two users click "Pay" at the same time — how do you prevent double charge?

### Authentication & Authorization

- 🟢 How does Laravel handle authentication?
- 🟢 Difference between `Auth::attempt()` vs `Auth::check()`?
- 🟡 What is a Guard? What are the default guards?
- 🟡 Difference between Policies vs Gates?
- 🟡 What is Laravel Sanctum? When to use it?
- 🟡 What is Laravel Passport? How is it different from Sanctum?
- 🔴 OAuth flow — how does it work?
- 🔴 How to handle API authentication in Laravel?
- 💡 How would you secure a Laravel API in production?
- 💡 Sanctum vs Passport — which would you choose for a mobile app backend?

### Security (Laravel-Specific)

- 🟡 How does CSRF protection work internally in Laravel?
- 🟡 What is XSS protection in Blade? (`{{ }}` vs `{!! !!}`)
- 🟡 What is Mass Assignment? (`$fillable` vs `$guarded`)
- 🔴 How to secure a Laravel application? (full checklist)
- 💡 Your app is vulnerable to mass assignment — how do you find and fix it?

### Validation & Exception Handling

- 🟢 How does validation work in Laravel?
- 🟢 Common validation rules? (`required`, `string`, `email`, `unique`, `exists`)
- 🟡 How to create custom validation rules?
- 🟡 What is Form Request validation? Why use it over inline validation?
- 🟡 How to handle exceptions globally in Laravel? (Handler class)
- 🟡 How to return custom API error responses?
- 💡 How do you validate complex nested JSON payloads?

### Events, Jobs & Queues

- 🟡 What are Events and Listeners in Laravel?
- 🟡 When would you use Events vs direct calls?
- 🟡 What are Jobs and Queues?
- 🟡 What queue drivers are available? (sync, database, Redis, SQS)
- 🔴 How do queues work internally? Worker lifecycle?
- 🔴 How do you handle failed jobs? Retry mechanisms? (`tries`, `backoff`, `failed()` method)
- 🔴 What is job batching? What about job chaining?
- 🔴 What is job middleware? (e.g., `RateLimited`, `WithoutOverlapping`)
- 🔴 Unique jobs vs rate-limited jobs — when to use each?
- 💡 Give a real example where you'd use a queue instead of handling synchronously.
- 💡 Your queue worker processes the same job twice — how do you make it idempotent?

### Caching, Redis & Performance

- 🟡 How does caching work in Laravel?
- 🟡 What cache drivers are available? (file, Redis, Memcached)
- 🟡 What is route caching? Config caching? View caching?
- 🟡 What is Redis? Use cases in Laravel? (cache, queues, rate limiting, sessions)
- 🟡 What are Laravel Collections? Difference between array vs collection?
- 🟡 Common collection methods: `map`, `filter`, `pluck`, `groupBy`, `reduce`
- 🔴 What are cache tags? When do you need them? (which drivers support them)
- 🔴 `Cache::remember()` vs `Cache::rememberForever()` vs `Cache::flexible()` (stale-while-revalidate)
- 🔴 What is cache stampede? How do you prevent it in Laravel? (`Cache::lock()`, atomic locks)
- 🔴 What is Laravel Octane? What does it change about the request lifecycle? (Swoole, FrankenPHP, RoadRunner — persistent app state)
- 🔴 How to optimize a Laravel application for production?
- 💡 When should you NOT cache?
- 💡 You moved to Octane and global state is now leaking between requests — why?

### Filesystem & Storage

- 🟡 How does Laravel Storage system work? (`Storage::disk()`)
- 🟡 Difference between local vs S3 storage?
- 🟡 How to handle file uploads in Laravel?
- 💡 How do you serve private files securely?

### API Development & Architecture

- 🟢 How to create a REST API in Laravel?
- 🟡 What are API Resources? (Resource classes for response transformation)
- 🟡 How to handle API versioning in Laravel?
- 🟡 How to implement pagination in API responses?
- 🟡 What is idempotency? PUT vs PATCH?
- 🟡 Difference between Entity vs DTO?
- 🔴 How to rate limit APIs in Laravel?
- 🔴 How does rate limiting work internally? (Redis/cache-based counters)
- 🔴 What is Service Layer pattern? How to structure it in Laravel?
- 🔴 What is Manager/Driver pattern?
- 💡 How do you structure a scalable Laravel application? (folder structure, patterns)
- 💡 How do you handle external API failures, retries, and circuit breaker?

### Logging & Monitoring

- 🟡 How does Laravel logging work? (Monolog)
- 🟡 What are log channels?
- 🔴 How to set up structured logging for production?
- 💡 How do you debug production issues in Laravel?

### Blade & Views

- 🟢 What are Blade directives?
- 🟡 What are Layouts and Components in Blade?
- 🟡 What are View Composers?
- 💡 When would a backend dev need to understand Blade?

### Testing in Laravel

- 🟢 Difference between Feature Tests vs Unit Tests?
- 🟡 How to write tests in Laravel? (`php artisan test`)
- 🟡 What is `RefreshDatabase` trait? `DatabaseTransactions` vs `RefreshDatabase` vs `DatabaseMigrations`?
- 🟡 How to test API endpoints?
- 🟡 What is Pest PHP? How is it different from PHPUnit? (modern test syntax — Laravel's new default)
- 🔴 What is mocking in Laravel tests? (`Mockery`, `Http::fake()`, `Queue::fake()`, `Event::fake()`)
- 🔴 What is PHPStan / LaraStan? Why use static analysis?
- 💡 How do you test code that depends on external APIs?

### Configuration, Deployment & CI/CD

- 🟡 How do you manage environment variables? (`.env` file)
- 🟡 How do you handle multiple environments in Laravel?
- 🟡 What is GitHub Actions? How to set up a basic CI pipeline?
- 🔴 How to set up a deployment pipeline for Laravel? (build, test, deploy)
- 🔴 What is zero-downtime deployment?
- 💡 Your app works locally but breaks in production — what do you check?
- 💡 You updated `.env` but the app doesn't reflect changes — why? (`config:cache` trap)

### Scaling & Infrastructure

- 🔴 How to scale a Laravel application?
- 🔴 What is horizontal scaling?
- 🔴 What is a load balancer?
- 🔴 Why must your app be stateless for scaling?
- 💡 Your app is getting 10x more traffic than usual — what do you do?

### Laravel Ecosystem & Modern Features

- 🟡 What is Laravel Horizon? (queue monitoring)
- 🟡 What is Laravel Telescope? (debugging)
- 🟡 What is Laravel Livewire? (conceptually)
- 🟡 What is Laravel Broadcasting? WebSockets? (conceptually)
- 🟡 What is Laravel Reverb? (first-party WebSocket server, Laravel 11+)
- 🟡 What is Laravel Pennant? (feature flags)
- 🟡 What is Laravel Octane? (Swoole, FrankenPHP, RoadRunner — when to use)
- 💡 When would you use events + broadcasting for realtime features?
- 💡 What new features in recent Laravel versions have you used?

### Project & Experience Questions (The Real Differentiator)

- 💡 Walk me through a complex feature you built end to end.
- 💡 Describe a production issue you debugged and fixed.
- 💡 How would you design a scalable Laravel system from scratch?
- 💡 What architecture decisions did you make in your last project and why?
- 💡 Tell me about a time you improved performance of an existing system.
- 💡 How do you decide between building vs using a package?