
---

### What is JaCoCo?

**Java Code Coverage** — a free library that measures how much of your code is covered by tests. It shows which lines, branches, and methods were executed during testing and which were missed.

---

### Why Use JaCoCo?

- **Visibility into test coverage** — see exactly what percentage of your code is tested
- **Identify dead code** — find code that's never executed
- **Focus on critical areas** — shows uncovered parts so you can prioritize testing efforts
- **Better maintenance** — coverage reports help maintain quality standards over time

---

### Setup — Maven Plugin

Add the JaCoCo Maven plugin to your `pom.xml`:

```xml
<build>
    <plugins>
        <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.12</version>
            <executions>
                <execution>
                    <goals>
                        <goal>prepare-agent</goal>
                    </goals>
                </execution>
                <execution>
                    <id>report</id>
                    <phase>test</phase>
                    <goals>
                        <goal>report</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
```

> Running tests with JUnit automatically activates the JaCoCo agent — no extra configuration needed.

---

### Running and Generating Reports

#### Option 1: From IDE

```
Right-click test package → Run All Tests with Coverage
       │
       ▼
IDE shows coverage table (lines, branches, methods)
       │
       ▼
Click the export/generate report option
       │
       ▼
Generates HTML report
```

#### Option 2: From Terminal (Maven)

```bash
./mvnw clean package
```

This does everything:

```
clean     → clears target folder
compile   → compiles source code
test      → runs all tests
package   → builds the JAR
       │
       ▼
JaCoCo agent runs during test phase
       │
       ▼
Generates:
  target/jacoco.exec              ← binary coverage data
  target/site/jacoco/index.html   ← HTML report
```

> Open `target/site/jacoco/index.html` in a browser to view the report.

---

### Reading the Report

The HTML report shows coverage at multiple levels:

```
Package level → Class level → Method level → Line level
```

|Metric|What It Measures|
|---|---|
|**Line coverage**|Percentage of code lines executed|
|**Branch coverage**|Percentage of if/else branches taken|
|**Method coverage**|Percentage of methods called|
|**Class coverage**|Percentage of classes touched|

In the report:

- **Green** — fully covered
- **Yellow** — partially covered (e.g., only one branch of an if/else)
- **Red** — not covered at all

---

### What Coverage to Aim For

|Coverage|Meaning|
|---|---|
|80%+|Good — industry standard target|
|90%+|Very good — critical systems|
|100%|Not practical — diminishing returns|

> High coverage doesn't guarantee good tests — you can have 100% coverage with meaningless assertions. Focus on **meaningful tests** for critical business logic, not just hitting a number.

---

### Quick Reference

- [Baeldung — JaCoCo Guide (Web)](https://www.baeldung.com/jacoco)